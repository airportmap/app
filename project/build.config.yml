project:
  name: "airportmap"
  version: "2.0.0"

paths:
  source: "./src"
  output: "./dist"
  temp: "./temp"

build:
  typescript:
    server:
      configFile: "./tsconfig.server.json"
    client:
      configFile: "./tsconfig.client.json"
